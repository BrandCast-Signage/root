name = "root:init"
description = "Initialize Root framework — detect project structure, configure directories, install templates, ingest docs into RAG"
type = "prompt"
prompt = "commands/root/init.md"
