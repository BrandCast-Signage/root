{
  "mcpServers": {
    "local-rag": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_DATA}/node_modules/mcp-local-rag/dist/index.js"],
      "env": {
        "DB_PATH": ".claude/rag-db",
        "CACHE_DIR": "${HOME}/.cache/mcp-local-rag/models"
      }
    }
  }
}
